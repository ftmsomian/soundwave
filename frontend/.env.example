NEXT_PUBLIC_APP_NAME=SoundWave

# آدرس بک‌اند برای اپ‌های ادغام‌شده (accounts). اگه ست نشه، به‌صورت پیش‌فرض
# http://localhost:8000/api/v1 استفاده می‌شه (یعنی همون چیزی که با
# `python manage.py runserver` روی پورت پیش‌فرض بالا میاد).
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
