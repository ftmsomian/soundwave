from django.urls import path

app_name = "accounts"

urlpatterns = [
    # TODO(member1): طبق docs/backend/api-contract.md پر کن
    # path("auth/register/", RegisterView.as_view()),
    # path("auth/login/", LoginView.as_view()),
    # path("users/me/", ProfileView.as_view()),
    # ...
]
